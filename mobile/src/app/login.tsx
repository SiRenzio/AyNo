import { Link, router } from 'expo-router'; import { useState } from 'react'; import { Alert, Text } from 'react-native'; import { Button, Field, Screen, colors, styles } from '@/components/native-ui'; import { useAuth } from '@/context/auth-context';
export default function Login() { const { signIn } = useAuth(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [busy, setBusy] = useState(false); async function submit() { setBusy(true); try { await signIn({ email, password }); router.replace('/(tabs)'); } catch (e) { Alert.alert('Could not sign in', e instanceof Error ? e.message : 'Try again.'); } finally { setBusy(false); } } return <Screen><Text style={[styles.title, { marginTop: 70 }]}>Welcome back</Text><Text style={styles.subtitle}>Your events, wherever the day takes you.</Text><Field autoCapitalize="none" keyboardType="email-address" placeholder="Email address" value={email} onChangeText={setEmail} /><Field secureTextEntry placeholder="Password" value={password} onChangeText={setPassword} /><Button title={busy ? 'Signing in…' : 'Sign in'} onPress={submit} disabled={busy || !email || !password} /><Text style={{ color: colors.muted, textAlign: 'center', marginTop: 24 }}>New to AyNo? <Link href="/register" style={{ color: colors.primary }}>Create an account</Link></Text></Screen>; }
