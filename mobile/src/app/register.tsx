import { Link, router } from 'expo-router'; import { useState } from 'react'; import { Alert, ScrollView, Text } from 'react-native'; import { Button, Field, Screen, colors, styles } from '@/components/native-ui'; import { useAuth } from '@/context/auth-context';
export default function Register() { const { signUp } = useAuth(); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [confirm, setConfirm] = useState(''); const [busy, setBusy] = useState(false); async function submit() { setBusy(true); try { await signUp({ name, email, password, password_confirmation: confirm }); router.replace('/(tabs)'); } catch (e) { Alert.alert('Could not register', e instanceof Error ? e.message : 'Try again.'); } finally { setBusy(false); } } return <Screen><ScrollView keyboardShouldPersistTaps="handled"><Text style={[styles.title, { marginTop: 30 }]}>Create account</Text><Text style={styles.subtitle}>Plan clearly and keep every reminder close.</Text><Field placeholder="Name" value={name} onChangeText={setName} /><Field autoCapitalize="none" keyboardType="email-address" placeholder="Email address" value={email} onChangeText={setEmail} /><Field secureTextEntry placeholder="Password" value={password} onChangeText={setPassword} /><Field secureTextEntry placeholder="Confirm password" value={confirm} onChangeText={setConfirm} /><Button title={busy ? 'Creating…' : 'Create account'} onPress={submit} disabled={busy || !name || !email || !password || password !== confirm} /><Text style={{ color: colors.muted, textAlign: 'center', marginTop: 24 }}>Already registered? <Link href="/login" style={{ color: colors.primary }}>Sign in</Link></Text></ScrollView></Screen>; }
