import { router } from 'expo-router'; import { Text, View } from 'react-native'; import { Button, Screen, colors, styles } from '@/components/native-ui'; import { useAuth } from '@/context/auth-context';
export default function Profile() { const { user, signOut } = useAuth(); return <Screen><Text style={styles.title}>Profile</Text><Text style={styles.subtitle}>Your AyNo account</Text><View style={styles.card}><Text style={{ color: colors.text, fontSize: 18, fontWeight: '700' }}>{user?.name}</Text><Text style={{ color: colors.muted, marginTop: 6 }}>{user?.email}</Text></View><Button title="Sign out" danger onPress={async () => { await signOut(); router.replace('/login'); }} /></Screen>; }
